VITE_API_BASE_URL=http://localhost:5050/api/v1
VITE_AI_TOKEN=virtual_life_ai_token_placeholder
# RevenueCat iOS public SDK key. Get this from the RevenueCat dashboard after
# creating the vl_* products in App Store Connect. Without it, in-app purchases
# will show a real error instead of silently succeeding.
VITE_REVENUECAT_IOS_API_KEY=
